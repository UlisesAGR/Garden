<resources>
    <!-- Base application theme. -->
    <style name="Theme.Garden" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="App.Starting.Theme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#0E150D</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/il_logo_dark</item>
        <item name="postSplashScreenTheme">@style/Theme.Garden</item>
    </style>

</resources>
